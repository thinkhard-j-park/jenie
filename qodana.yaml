version: "1.0"
linter: jetbrains/qodana-jvm:2024.2
checks:
  - id: "Unit tests"
    name: "jenie-spring-data-mongodb"
    run:
      - name: "Unit tests"
        command: ./jenie-spring-data-mongodb/gradlew -b ./jenie-spring-data-mongodb/build.gradle test
profile:
  name: qodana.recommended
include:
  - name: CheckDependencyLicenses