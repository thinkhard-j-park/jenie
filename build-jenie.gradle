List<String> protobufProjects = ['jenie-spring-helloworld-common']
List<String> bootProjects = [
		'jenie-spring-data-mongodb',
		'jenie-spring-data-mongodb-reactive',
		'jenie-spring-helloworld-web',
		'jenie-spring-helloworld-rest',
		'jenie-spring-helloworld-rest-reactive',
		'jenie-spring-helloworld-grpc',
		'jenie-spring-helloworld-grpc-reactive',
		'jenie-spring-helloworld-armeria-grpc',
		'jenie-spring-helloworld-armeria-grpc-reactive',
		'jenie-spring-test'
]
List<String> svcProjects = [
		'jenie-spring-helloworld-rest',
		'jenie-spring-helloworld-rest-reactive',
		'jenie-spring-helloworld-grpc',
		'jenie-spring-helloworld-grpc-reactive',
		'jenie-spring-helloworld-armeria-grpc',
		'jenie-spring-helloworld-armeria-grpc-reactive'
]
List<String> testProjects = ['jenie-spring-test']

ext {
	protobufProjs = subprojects.findAll { Project p -> p.name in protobufProjects }
	libProjs = subprojects.findAll { Project p -> p.name !in svcProjects }
	bootProjs = subprojects.findAll { Project p -> p.name in bootProjects }
	svcProjs = subprojects.findAll { Project p -> p.name in svcProjects }
	mainProjs = subprojects.findAll { Project p -> p.name !in testProjects }
	testProjs = subprojects.findAll { Project p -> p.name in testProjects }
	jacocoProjs = subprojects.findAll { Project p -> p.name !in testProjects }
}
